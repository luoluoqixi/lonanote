diff --git a/node_modules/expo-modules-autolinking/.bun-tag-7108650b0516cfc b/.bun-tag-7108650b0516cfc
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/build/commands/generateModulesProviderCommand.js b/build/commands/generateModulesProviderCommand.js
index b275bb85baee81b98a4a0b01e97dbb8c968d2899..92e5fd834236eeda91b9fe04263e7231d15ca058 100644
--- a/build/commands/generateModulesProviderCommand.js
+++ b/build/commands/generateModulesProviderCommand.js
@@ -14,7 +14,7 @@ function generateModulesProviderCommand(cli) {
     return (0, autolinkingOptions_1.registerAutolinkingArguments)(cli.command('generate-modules-provider [searchPaths...]'))
         .option('-t, --target <path>', 'Path to the target file, where the package list should be written to.')
         .option('--entitlement <path>', 'Path to the Apple code signing entitlements file.')
-        .option('-p, --packages <packages...>', 'Names of the packages to include in the generated modules provider.')
+        .option('--packages <packages...>', 'Names of the packages to include in the generated modules provider.')
         .option('--app-root <path>', 'Path to the app root directory.')
         .option('--podfile-properties-file-path <path>', 'Path to the Podfile properties file.')
         .action(async (searchPaths, commandArguments) => {
diff --git a/build/commands/reactNativeConfigCommand.js b/build/commands/reactNativeConfigCommand.js
index a942305a5dac9d85965745d92c6ba1c57eca4dd6..c357b646a3f494dc5b969ebea6211d704424d195 100644
--- a/build/commands/reactNativeConfigCommand.js
+++ b/build/commands/reactNativeConfigCommand.js
@@ -6,12 +6,12 @@ const reactNativeConfig_1 = require("../reactNativeConfig");
 /** The react-native-config command (like RN CLI linking) */
 function reactNativeConfigCommand(cli) {
     return (0, autolinkingOptions_1.registerAutolinkingArguments)(cli.command('react-native-config [searchPaths...]'))
-        .option('-p, --platform [platform]', 'The platform that the resulting modules must support. Available options: "android", "ios"', 'ios')
         .option('--source-dir <sourceDir>', 'The path to the native source directory')
         .option('-j, --json', 'Output results in the plain JSON format.', () => true, false)
         .action(async (searchPaths, commandArguments) => {
-        // TODO(@kitten): Do we need to restrict this?
-        const platform = commandArguments.platform ?? 'ios';
+        // NOTE: registerAutolinkingArguments registers -p, --platform with default 'apple',
+        // but the react-native-config command defaults to 'ios' if not explicitly provided.
+        const platform = (commandArguments.platform && commandArguments.platform !== 'apple' ? commandArguments.platform : 'ios') ?? 'ios';
         if (platform !== 'android' && platform !== 'ios') {
             throw new Error(`Unsupported platform: ${platform}`);
         }
diff --git a/build/commands/verifyCommand.js b/build/commands/verifyCommand.js
index cc47df9b3360ec1ed5e3a45c0aae75eaaaf878ff..50337b2f53d376fb0e999647ac0157a0742f3f84 100644
--- a/build/commands/verifyCommand.js
+++ b/build/commands/verifyCommand.js
@@ -17,18 +17,23 @@ function verifyCommand(cli) {
     return (0, autolinkingOptions_1.registerAutolinkingArguments)(cli.command('verify'))
         .option('-v, --verbose', 'Output all results instead of just warnings.', () => true, false)
         .option('-j, --json', 'Output results in the plain JSON format.', () => true, false)
-        .option('-p, --platform [platform]', `The platform to validate native modules for. Available options: ${[...AUTOLINKING_PLATFORMS, 'native', 'all'].map((x) => `"${x}"`).join(', ')}`, 'all')
         .action(async (commandArguments) => {
+        // NOTE: registerAutolinkingArguments registers -p, --platform with default 'apple',
+        // but the verify command defaults to 'all' if not explicitly provided.
+        // 'apple' from the default means "all platforms" for verify purposes.
+        let platform = commandArguments.platform;
+        if (!platform || platform === 'apple') {
+            platform = 'all';
+        }
         let platforms;
-        // NOTE(@kitten): Preserve `both` for backwards-compatibility
-        if (commandArguments.platform === 'both' || commandArguments.platform === 'native') {
+        if (platform === 'both' || platform === 'native') {
             platforms = ['android', 'ios'];
         }
-        else if (commandArguments.platform === 'all') {
+        else if (platform === 'all') {
             platforms = AUTOLINKING_PLATFORMS;
         }
         else {
-            platforms = [commandArguments.platform];
+            platforms = [platform];
         }
         const autolinkingOptionsLoader = (0, autolinkingOptions_1.createAutolinkingOptionsLoader)(commandArguments);
         const appRoot = await autolinkingOptionsLoader.getAppRoot();
diff --git a/build/index.js b/build/index.js
index 1db799b6450a6b43d4d593f34e70ae2fe0ac8139..9e4625517e52ed8b2b5e49b47d17f4fba727ef8c 100644
--- a/build/index.js
+++ b/build/index.js
@@ -3,7 +3,7 @@ var __importDefault = (this && this.__importDefault) || function (mod) {
     return (mod && mod.__esModule) ? mod : { "default": mod };
 };
 Object.defineProperty(exports, "__esModule", { value: true });
-const commander_1 = __importDefault(require("commander"));
+const { Command } = require("commander");
 const generateModulesProviderCommand_1 = require("./commands/generateModulesProviderCommand");
 const mirrorKotlinInlineModulesCommand_1 = require("./commands/mirrorKotlinInlineModulesCommand");
 const reactNativeConfigCommand_1 = require("./commands/reactNativeConfigCommand");
@@ -12,7 +12,7 @@ const searchCommand_1 = require("./commands/searchCommand");
 const verifyCommand_1 = require("./commands/verifyCommand");
 const memoize_1 = require("./memoize");
 async function main(args) {
-    const cli = commander_1.default
+    const cli = new Command()
         .version(require('expo-modules-autolinking/package.json').version)
         .description('CLI command that searches for native modules to autolink them.');
     (0, verifyCommand_1.verifyCommand)(cli);
