diff --git a/lib/module/ReducedMotion.js b/lib/module/ReducedMotion.js
index 3074396..c2159da 100644
--- a/lib/module/ReducedMotion.js
+++ b/lib/module/ReducedMotion.js
@@ -3,9 +3,7 @@
 import { IS_WEB, IS_WINDOW_AVAILABLE } from "./common/index.js";
 import { makeMutable } from "./mutables.js";
 export function isReducedMotionEnabledInSystem() {
-  return IS_WEB ? IS_WINDOW_AVAILABLE ?
-  // @ts-ignore Fallback if `window` is undefined.
-  window.matchMedia('(prefers-reduced-motion: reduce)').matches : false : !!global._REANIMATED_IS_REDUCED_MOTION;
+  return IS_WEB ? false : !!global._REANIMATED_IS_REDUCED_MOTION;
 }
 const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
 export const ReducedMotionManager = {
diff --git a/lib/module/core.js b/lib/module/core.js
index 74d8dc5..7a1d60a 100644
--- a/lib/module/core.js
+++ b/lib/module/core.js
@@ -56,7 +56,7 @@ export function registerEventHandler(eventHandler, eventName, emitterReactTag =
 
     eventHandler(event);
     // We call mappers here to make sure view updates can be applied in the same frame after an event.
-    global.__mapperRun();
+    global.__mapperRun?.();
   }
   return ReanimatedModule.registerEventHandler(createSerializable(handleEvent), eventName, emitterReactTag);
 }
@@ -71,7 +71,7 @@ export function subscribeForKeyboardEvents(eventHandler, options) {
 
     eventHandler(state, height);
     // We call mappers here to make sure view updates can be applied in the same frame after an event.
-    global.__mapperRun();
+    global.__mapperRun?.();
   }
   if (__DEV__) {
     controlEdgeToEdgeValues({
diff --git a/lib/module/hook/useReducedMotion.js b/lib/module/hook/useReducedMotion.js
index ee25aef..b02a1f9 100644
--- a/lib/module/hook/useReducedMotion.js
+++ b/lib/module/hook/useReducedMotion.js
@@ -1,7 +1,6 @@
 'use strict';
 
-import { isReducedMotionEnabledInSystem } from "../ReducedMotion.js";
-const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
+import { ReducedMotionManager } from "../ReducedMotion.js";
 
 // is-tree-shakable-suppress
 /**
@@ -15,6 +14,6 @@ const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
  * @see https://docs.swmansion.com/react-native-reanimated/docs/device/useReducedMotion
  */
 export function useReducedMotion() {
-  return IS_REDUCED_MOTION_ENABLED_IN_SYSTEM;
+  return ReducedMotionManager.jsValue;
 }
 //# sourceMappingURL=useReducedMotion.js.map
\ No newline at end of file
diff --git a/lib/module/mappers.js b/lib/module/mappers.js
index f89b258..5889406 100644
--- a/lib/module/mappers.js
+++ b/lib/module/mappers.js
@@ -218,4 +218,10 @@ export function stopMapper(mapperID) {
     mapperRegistry?.stop(mapperID);
   });
 }
+scheduleOnUI(() => {
+  'worklet';
+  if (global.__mapperRegistry === undefined) {
+    global.__mapperRegistry = createMapperRegistry();
+  }
+});
 //# sourceMappingURL=mappers.js.map
\ No newline at end of file
diff --git a/src/ReducedMotion.ts b/src/ReducedMotion.ts
index 19f863c..8794e40 100644
--- a/src/ReducedMotion.ts
+++ b/src/ReducedMotion.ts
@@ -6,10 +6,7 @@ type localGlobal = typeof global & Record<string, unknown>;
 
 export function isReducedMotionEnabledInSystem() {
   return IS_WEB
-    ? IS_WINDOW_AVAILABLE
-      ? // @ts-ignore Fallback if `window` is undefined.
-        window.matchMedia('(prefers-reduced-motion: reduce)').matches
-      : false
+    ? false
     : !!(global as localGlobal)._REANIMATED_IS_REDUCED_MOTION;
 }
 
diff --git a/src/core.ts b/src/core.ts
index a18b111..ae69beb 100644
--- a/src/core.ts
+++ b/src/core.ts
@@ -91,7 +91,7 @@ export function registerEventHandler<TEvent>(
     'worklet';
     eventHandler(event);
     // We call mappers here to make sure view updates can be applied in the same frame after an event.
-    global.__mapperRun();
+    global.__mapperRun?.();
   }
   return ReanimatedModule.registerEventHandler(
     createSerializable(handleEvent as WorkletFunction),
@@ -114,7 +114,7 @@ export function subscribeForKeyboardEvents(
     'worklet';
     eventHandler(state, height);
     // We call mappers here to make sure view updates can be applied in the same frame after an event.
-    global.__mapperRun();
+    global.__mapperRun?.();
   }
 
   if (__DEV__) {
diff --git a/src/hook/useReducedMotion.ts b/src/hook/useReducedMotion.ts
index 7cc1fd6..ea55b25 100644
--- a/src/hook/useReducedMotion.ts
+++ b/src/hook/useReducedMotion.ts
@@ -1,7 +1,6 @@
 'use strict';
-import { isReducedMotionEnabledInSystem } from '../ReducedMotion';
+import { ReducedMotionManager } from '../ReducedMotion';
 
-const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
 
 // is-tree-shakable-suppress
 /**
@@ -15,5 +14,5 @@ const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
  * @see https://docs.swmansion.com/react-native-reanimated/docs/device/useReducedMotion
  */
 export function useReducedMotion() {
-  return IS_REDUCED_MOTION_ENABLED_IN_SYSTEM;
+  return ReducedMotionManager.jsValue;
 }
diff --git a/src/mappers.ts b/src/mappers.ts
index a315683..ad4640c 100644
--- a/src/mappers.ts
+++ b/src/mappers.ts
@@ -265,3 +265,10 @@ export function stopMapper(mapperID: number): void {
     mapperRegistry?.stop(mapperID);
   });
 }
+
+scheduleOnUI(() => {
+  'worklet';
+  if (global.__mapperRegistry === undefined) {
+    global.__mapperRegistry = createMapperRegistry();
+  }
+});
