# react-native-webview iOS 滚动条 inset 补丁说明：
# - 增加 `scrollIndicatorInsets`，直接设置 WKWebView.scrollView 的原生滚动条 inset。
# - 用于让透明 header、底部安全区和键盘遮挡区避让滚动条，不改变 HTML 内容布局。
# - Fabric 与旧架构均透传该属性；原生 layout 后会重新应用，避免系统布局覆盖。
#
# react-native-webview 移动端输入法补丁说明：
# - 增加 `inputMethodEnabled`，关闭时保留 WebView/HTML 编辑器焦点与选区，仅隐藏软键盘。
# - iOS 通过自定义 inputView 切换，Android 通过 InputConnection 与 InputMethodManager 切换。
# - Fabric 与旧架构均透传该属性，默认值为 true，不改变未使用该属性时的上游行为。
#
# react-native-webview iOS 键盘聚焦滚动补丁说明：
# - EditorWebView 启用 `removeIosKeyboardObserver` 时，在键盘出现期间锁定 WKWebView 的滚动位置。
# - 用于拦截 iOS 26 仍会触发的聚焦元素自动滚动，避免键盘动画末尾页面轻微跳动。
# - `keyboardDidShow` 后延迟 250ms 解锁，`keyboardWillHide` 立即解锁；正常浏览滚动不受影响。

diff --git a/src/WebViewTypes.ts b/src/WebViewTypes.ts
index 4e7247f8..2a84f9ab 100644
--- a/src/WebViewTypes.ts
+++ b/src/WebViewTypes.ts
@@ -438,6 +438,12 @@ export interface IOSWebViewProps extends WebViewSharedProps {
    */
   automaticallyAdjustsScrollIndicatorInsets?: boolean;

+  /**
+   * 设置 iOS 原生滚动条的 inset，不会改变 WebView 内容的布局。
+   * @platform ios
+   */
+  scrollIndicatorInsets?: ContentInsetProp;
+
   /**
    * This property specifies how the safe area insets are used to modify the
    * content area of the scroll view. The default value of this property is
@@ -1172,6 +1178,15 @@ export interface AndroidWebViewProps extends WebViewSharedProps {
 }

 export interface WebViewSharedProps extends ViewProps {
+  /**
+   * Controls whether the focused WebView can present the system input method.
+   * Disabling it keeps the focused HTML element and selection intact.
+   * The default value is `true`.
+   * @platform android
+   * @platform ios
+   */
+  inputMethodEnabled?: boolean;
+
   /**
    * Loads static html or a uri (with optional headers) in the WebView.
    */
diff --git a/src/RNCWebViewNativeComponent.ts b/src/RNCWebViewNativeComponent.ts
index ca3af151..2107b385 100644
--- a/src/RNCWebViewNativeComponent.ts
+++ b/src/RNCWebViewNativeComponent.ts
@@ -131,6 +131,8 @@ type WebViewDownloadEvent = Readonly<{
 // type MenuItem = Readonly<{label: string, key: string}>;

 export interface NativeProps extends ViewProps {
+  inputMethodEnabled?: WithDefault<boolean, true>;
+
   // Android only
   allowFileAccess?: boolean;
   allowsProtectedMedia?: boolean;
@@ -179,6 +181,12 @@ export interface NativeProps extends ViewProps {
     bottom?: Double;
     right?: Double;
   }>;
+  scrollIndicatorInsets?: Readonly<{
+    top?: Double;
+    left?: Double;
+    bottom?: Double;
+    right?: Double;
+  }>;
   contentInsetAdjustmentBehavior?: WithDefault<
     'never' | 'automatic' | 'scrollableAxes' | 'always',
     'never'
diff --git a/lib/WebViewTypes.d.ts b/lib/WebViewTypes.d.ts
index f666e814..1ef3eb48 100644
--- a/lib/WebViewTypes.d.ts
+++ b/lib/WebViewTypes.d.ts
@@ -316,6 +316,11 @@ export interface IOSWebViewProps extends WebViewSharedProps {
      * @platform ios
      */
     automaticallyAdjustsScrollIndicatorInsets?: boolean;
+    /**
+     * 设置 iOS 原生滚动条的 inset，不会改变 WebView 内容的布局。
+     * @platform ios
+     */
+    scrollIndicatorInsets?: ContentInsetProp;
     /**
      * This property specifies how the safe area insets are used to modify the
      * content area of the scroll view. The default value of this property is
@@ -974,6 +979,14 @@ export interface AndroidWebViewProps extends WebViewSharedProps {
     onLoadSubResourceError?: (event: WebViewErrorEvent) => void;
 }
 export interface WebViewSharedProps extends ViewProps {
+    /**
+     * Controls whether the focused WebView can present the system input method.
+     * Disabling it keeps the focused HTML element and selection intact.
+     * The default value is `true`.
+     * @platform android
+     * @platform ios
+     */
+    inputMethodEnabled?: boolean;
     /**
      * Loads static html or a uri (with optional headers) in the WebView.
      */
diff --git a/lib/RNCWebViewNativeComponent.d.ts b/lib/RNCWebViewNativeComponent.d.ts
index 6b789f98..1fbd430c 100644
--- a/lib/RNCWebViewNativeComponent.d.ts
+++ b/lib/RNCWebViewNativeComponent.d.ts
@@ -113,6 +113,7 @@ type WebViewDownloadEvent = Readonly<{
     downloadUrl: string;
 }>;
 export interface NativeProps extends ViewProps {
+    inputMethodEnabled?: WithDefault<boolean, true>;
     allowFileAccess?: boolean;
     allowsProtectedMedia?: boolean;
     allowsFullscreenVideo?: boolean;
@@ -153,6 +154,12 @@ export interface NativeProps extends ViewProps {
         bottom?: Double;
         right?: Double;
     }>;
+    scrollIndicatorInsets?: Readonly<{
+        top?: Double;
+        left?: Double;
+        bottom?: Double;
+        right?: Double;
+    }>;
     contentInsetAdjustmentBehavior?: WithDefault<'never' | 'automatic' | 'scrollableAxes' | 'always', 'never'>;
     contentMode?: WithDefault<'recommended' | 'mobile' | 'desktop', 'recommended'>;
     dataDetectorTypes?: WithDefault<ReadonlyArray<'address' | 'link' | 'calendarEvent' | 'trackingNumber' | 'flightNumber' | 'lookupSuggestion' | 'phoneNumber' | 'all' | 'none'>, 'phoneNumber'>;
diff --git a/apple/RNCWebViewImpl.h b/apple/RNCWebViewImpl.h
index 1f637c1d..ccd815cd 100644
--- a/apple/RNCWebViewImpl.h
+++ b/apple/RNCWebViewImpl.h
@@ -86,7 +86,9 @@ shouldStartLoadForRequest:(NSMutableDictionary<NSString *, id> *)request
 @property (nonatomic, assign) BOOL bounces;
 @property (nonatomic, assign) BOOL mediaPlaybackRequiresUserAction;
 @property (nonatomic, assign) UIEdgeInsets contentInset;
+@property (nonatomic, assign) UIEdgeInsets scrollIndicatorInsets;
 @property (nonatomic, assign) BOOL automaticallyAdjustContentInsets;
+@property (nonatomic, assign) BOOL inputMethodEnabled;
 @property (nonatomic, assign) BOOL keyboardDisplayRequiresUserAction;
 @property (nonatomic, assign) BOOL hideKeyboardAccessoryView;
 @property (nonatomic, assign) BOOL removeIosKeyboardObserver;
diff --git a/apple/RNCWebViewImpl.m b/apple/RNCWebViewImpl.m
index 38904266..a65f7650 100644
--- a/apple/RNCWebViewImpl.m
+++ b/apple/RNCWebViewImpl.m
@@ -52,10 +52,24 @@ @interface RNCWKWebView : WKWebView
 #if !TARGET_OS_OSX
 @property (nonatomic, copy) NSArray<NSDictionary *> * _Nullable menuItems;
 @property (nonatomic, copy) NSArray<NSString *> * _Nullable suppressMenuItems;
+@property (nonatomic, assign) BOOL inputMethodEnabled;
 #endif // !TARGET_OS_OSX
 @end
 @implementation RNCWKWebView
 #if !TARGET_OS_OSX
+- (UIView *)inputView
+{
+  return self.inputMethodEnabled ? [super inputView] : [UIView new];
+}
+
+- (void)setInputMethodEnabled:(BOOL)inputMethodEnabled
+{
+  _inputMethodEnabled = inputMethodEnabled;
+  for (UIView *subview in self.scrollView.subviews) {
+    [subview reloadInputViews];
+  }
+}
+
 - (NSString *)stringFromAction:(SEL) action {
   NSString *sel = NSStringFromSelector(action);

@@ -145,6 +159,12 @@ @implementation RNCWebViewImpl
   UIStatusBarStyle _savedStatusBarStyle;
 #endif // !TARGET_OS_OSX
   BOOL _savedStatusBarHidden;
+#if TARGET_OS_IOS
+  CGPoint _keyboardScrollOffset;
+  BOOL _keyboardScrollLockEnabled;
+  BOOL _restoringKeyboardScrollOffset;
+  NSUInteger _keyboardScrollLockGeneration;
+#endif // TARGET_OS_IOS

 #if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) && __IPHONE_OS_VERSION_MAX_ALLOWED >= 110000 /* __IPHONE_11_0 */
   UIScrollViewContentInsetAdjustmentBehavior _savedContentInsetAdjustmentBehavior;
@@ -175,6 +195,8 @@ - (instancetype)initWithFrame:(CGRect)frame
     _automaticallyAdjustContentInsets = YES;
     _autoManageStatusBarEnabled = YES;
     _contentInset = UIEdgeInsetsZero;
+    _scrollIndicatorInsets = UIEdgeInsetsZero;
+    _inputMethodEnabled = YES;
     _savedKeyboardDisplayRequiresUserAction = YES;
     _injectedJavaScript = nil;
     _injectedJavaScriptForMainFrameOnly = YES;
@@ -227,6 +249,10 @@ - (instancetype)initWithFrame:(CGRect)frame
      addObserver:self
      selector:@selector(keyboardWillShow)
      name:UIKeyboardWillShowNotification object:nil];
+    [[NSNotificationCenter defaultCenter]
+     addObserver:self
+     selector:@selector(keyboardDidShow)
+     name:UIKeyboardDidShowNotification object:nil];

     // Workaround for StatusBar appearance bug for iOS 12
     // https://github.com/react-native-webview/react-native-webview/issues/62
@@ -423,6 +449,13 @@ - (void)webView:(WKWebView *)webView runOpenPanelWithParameters:(WKOpenPanelPara
 - (WKWebViewConfiguration *)setUpWkWebViewConfig
 {
   WKWebViewConfiguration *wkWebViewConfig = [WKWebViewConfiguration new];
+#if TARGET_OS_IOS
+  Class resourceHandlerClass = NSClassFromString(@"LonanoteResourceSchemeHandler");
+  if (resourceHandlerClass != Nil && [resourceHandlerClass conformsToProtocol:@protocol(WKURLSchemeHandler)]) {
+    id<WKURLSchemeHandler> resourceHandler = [[resourceHandlerClass alloc] init];
+    [wkWebViewConfig setURLSchemeHandler:resourceHandler forURLScheme:@"lonanote-resource"];
+  }
+#endif
   WKPreferences *prefs = [[WKPreferences alloc]init];
   BOOL _prefsUsed = NO;
   if (!_javaScriptEnabled) {
@@ -519,6 +552,7 @@ - (void)didMoveToWindow
     _webView = [[RNCWKWebView alloc] initWithFrame:self.bounds configuration: wkWebViewConfig];
     [self setBackgroundColor: _savedBackgroundColor];
 #if !TARGET_OS_OSX
+    _webView.inputMethodEnabled = _inputMethodEnabled;
     // Apply once at creation time. The prop is documented as non-reactive —
     // toggling it after mount cannot restore the observers WKWebView registers internally
     if (_removeIosKeyboardObserver) {
@@ -578,6 +612,10 @@ - (void)didMoveToWindow
     }
 #endif

+#if !TARGET_OS_OSX
+    _webView.scrollView.scrollIndicatorInsets = _scrollIndicatorInsets;
+#endif
+
 #if __MAC_OS_X_VERSION_MAX_ALLOWED >= 130300 || \
     __IPHONE_OS_VERSION_MAX_ALLOWED >= 160400 || \
     __TV_OS_VERSION_MAX_ALLOWED >= 160400
@@ -688,15 +726,35 @@ -(void)hideFullScreenVideoStatusBars

 -(void)keyboardWillHide
 {
+  _keyboardScrollLockGeneration += 1;
+  _keyboardScrollLockEnabled = NO;
   keyboardTimer = [NSTimer scheduledTimerWithTimeInterval:0 target:self selector:@selector(keyboardDisplacementFix) userInfo:nil repeats:false];
   [[NSRunLoop mainRunLoop] addTimer:keyboardTimer forMode:NSRunLoopCommonModes];
 }
 -(void)keyboardWillShow
 {
+  if (_removeIosKeyboardObserver && _webView != nil) {
+    _keyboardScrollLockGeneration += 1;
+    _keyboardScrollOffset = _webView.scrollView.contentOffset;
+    _keyboardScrollLockEnabled = YES;
+  }
   if (keyboardTimer != nil) {
     [keyboardTimer invalidate];
   }
 }
+-(void)keyboardDidShow
+{
+  if (!_keyboardScrollLockEnabled) {
+    return;
+  }
+
+  NSUInteger generation = ++_keyboardScrollLockGeneration;
+  dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(0.25 * NSEC_PER_SEC)), dispatch_get_main_queue(), ^{
+    if (self->_keyboardScrollLockGeneration == generation) {
+      self->_keyboardScrollLockEnabled = NO;
+    }
+  });
+}
 -(void)keyboardDisplacementFix
 {
   // Additional viewport checks to prevent unintentional scrolls
@@ -837,6 +895,26 @@ - (void)setContentInset:(UIEdgeInsets)contentInset
 #endif // !TARGET_OS_OSX
 }

+- (void)setScrollIndicatorInsets:(UIEdgeInsets)scrollIndicatorInsets
+{
+  _scrollIndicatorInsets = scrollIndicatorInsets;
+#if !TARGET_OS_OSX
+  if (_webView != nil) {
+    _webView.scrollView.scrollIndicatorInsets = scrollIndicatorInsets;
+  }
+#endif // !TARGET_OS_OSX
+}
+
+- (void)setInputMethodEnabled:(BOOL)inputMethodEnabled
+{
+  _inputMethodEnabled = inputMethodEnabled;
+#if !TARGET_OS_OSX
+  if (_webView != nil) {
+    _webView.inputMethodEnabled = inputMethodEnabled;
+  }
+#endif // !TARGET_OS_OSX
+}
+
 - (void)refreshContentInset
 {
 #if !TARGET_OS_OSX
@@ -1060,6 +1138,18 @@ - (void)setScrollEnabled:(BOOL)scrollEnabled
 // UIScrollViewDelegate method
 - (void)scrollViewDidScroll:(UIScrollView *)scrollView
 {
+#if TARGET_OS_IOS
+  // iOS 26 仍可能通过 WKWebView 内部路径滚动聚焦元素；键盘动画期间保持原偏移，避免页面轻微跳动。
+  if (_keyboardScrollLockEnabled) {
+    if (!_restoringKeyboardScrollOffset && !CGPointEqualToPoint(scrollView.contentOffset, _keyboardScrollOffset)) {
+      _restoringKeyboardScrollOffset = YES;
+      scrollView.contentOffset = _keyboardScrollOffset;
+      _restoringKeyboardScrollOffset = NO;
+    }
+    return;
+  }
+#endif // TARGET_OS_IOS
+
   // Don't allow scrolling the scrollView.
   if (!_scrollEnabled) {
     scrollView.bounds = _webView.bounds;
@@ -1140,5 +1230,6 @@ - (void)layoutSubviews
   _webView.frame = self.bounds;
 #if !TARGET_OS_OSX
   _webView.scrollView.contentInset = _contentInset;
+  _webView.scrollView.scrollIndicatorInsets = _scrollIndicatorInsets;
 #endif // !TARGET_OS_OSX
 }
diff --git a/apple/RNCWebView.mm b/apple/RNCWebView.mm
index 2be78778..729a499c 100644
--- a/apple/RNCWebView.mm
+++ b/apple/RNCWebView.mm
@@ -307,6 +307,7 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
     REMAP_WEBVIEW_PROP(automaticallyAdjustContentInsets)
     REMAP_WEBVIEW_PROP(autoManageStatusBarEnabled)
     REMAP_WEBVIEW_PROP(hideKeyboardAccessoryView)
+    REMAP_WEBVIEW_PROP(inputMethodEnabled)
     REMAP_WEBVIEW_PROP(allowsBackForwardNavigationGestures)
     REMAP_WEBVIEW_PROP(incognito)
     REMAP_WEBVIEW_PROP(pagingEnabled)
@@ -382,6 +383,16 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
         [_view setContentInset: edgesInsets];
     }

+    if (oldViewProps.scrollIndicatorInsets.top != newViewProps.scrollIndicatorInsets.top || oldViewProps.scrollIndicatorInsets.left != newViewProps.scrollIndicatorInsets.left || oldViewProps.scrollIndicatorInsets.right != newViewProps.scrollIndicatorInsets.right || oldViewProps.scrollIndicatorInsets.bottom != newViewProps.scrollIndicatorInsets.bottom) {
+        UIEdgeInsets edgesInsets = {
+            .top = newViewProps.scrollIndicatorInsets.top,
+            .left = newViewProps.scrollIndicatorInsets.left,
+            .right = newViewProps.scrollIndicatorInsets.right,
+            .bottom = newViewProps.scrollIndicatorInsets.bottom
+        };
+        [_view setScrollIndicatorInsets: edgesInsets];
+    }
+
     if (oldViewProps.basicAuthCredential.username != newViewProps.basicAuthCredential.username || oldViewProps.basicAuthCredential.password != newViewProps.basicAuthCredential.password) {
         [_view setBasicAuthCredential: @{
             @"username": RCTNSStringFromString(newViewProps.basicAuthCredential.username),
diff --git a/apple/RNCWebViewManager.mm b/apple/RNCWebViewManager.mm
index a510ffd9..b61109c4 100644
--- a/apple/RNCWebViewManager.mm
+++ b/apple/RNCWebViewManager.mm
@@ -68,6 +68,8 @@ - (RNCView *)view
 RCT_EXPORT_VIEW_PROPERTY(mediaPlaybackRequiresUserAction, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(dataDetectorTypes, WKDataDetectorTypes)
 RCT_EXPORT_VIEW_PROPERTY(contentInset, UIEdgeInsets)
+RCT_EXPORT_VIEW_PROPERTY(scrollIndicatorInsets, UIEdgeInsets)
+RCT_EXPORT_VIEW_PROPERTY(inputMethodEnabled, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(automaticallyAdjustContentInsets, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(autoManageStatusBarEnabled, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(hideKeyboardAccessoryView, BOOL)
diff --git a/android/src/newarch/com/reactnativecommunity/webview/RNCWebViewManager.java b/android/src/newarch/com/reactnativecommunity/webview/RNCWebViewManager.java
index fa8efb88..a6ae6b26 100644
--- a/android/src/newarch/com/reactnativecommunity/webview/RNCWebViewManager.java
+++ b/android/src/newarch/com/reactnativecommunity/webview/RNCWebViewManager.java
@@ -202,6 +202,12 @@ public class RNCWebViewManager extends ViewGroupManager<RNCWebViewWrapper>
         mRNCWebViewManagerImpl.setJavaScriptEnabled(view, enabled);
     }

+    @Override
+    @ReactProp(name = "inputMethodEnabled", defaultBoolean = true)
+    public void setInputMethodEnabled(RNCWebViewWrapper view, boolean value) {
+        mRNCWebViewManagerImpl.setInputMethodEnabled(view, value);
+    }
+
     @Override
     @ReactProp(name = "lackPermissionToDownloadMessage")
     public void setLackPermissionToDownloadMessage(RNCWebViewWrapper view, @Nullable String value) {
@@ -369,6 +375,9 @@ public class RNCWebViewManager extends ViewGroupManager<RNCWebViewWrapper>
     @Override
     public void setContentInset(RNCWebViewWrapper view, @Nullable ReadableMap value) {}

+    @Override
+    public void setScrollIndicatorInsets(RNCWebViewWrapper view, @Nullable ReadableMap value) {}
+
     @Override
     public void setContentInsetAdjustmentBehavior(RNCWebViewWrapper view, @Nullable String value) {}

@@ -564,4 +573,4 @@ public class RNCWebViewManager extends ViewGroupManager<RNCWebViewWrapper>
         mRNCWebViewManagerImpl.onDropViewInstance(view);
         super.onDropViewInstance(view);
     }
-}
\ No newline at end of file
+}
diff --git a/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java b/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java
index 209b8552..4d48778d 100644
--- a/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java
+++ b/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java
@@ -32,6 +32,7 @@ import com.reactnativecommunity.webview.events.TopRenderProcessGoneEvent;
 import com.reactnativecommunity.webview.events.TopShouldStartLoadWithRequestEvent;
 import android.webkit.CookieManager;

+import java.lang.reflect.Method;
 import java.util.concurrent.atomic.AtomicReference;

 public class RNCWebViewClient extends WebViewClient {
@@ -41,6 +42,39 @@ public class RNCWebViewClient extends WebViewClient {
     protected boolean mLastLoadFailed = false;
     protected RNCWebView.ProgressChangedFilter progressChangedFilter = null;
     protected @Nullable RNCBasicAuthCredential basicAuthCredential = null;
+    private static final String LONANOTE_RESOURCE_SCHEME = "lonanote-resource";
+    private static boolean resourceGatewayResolutionAttempted = false;
+    protected static @Nullable Method resourceGatewayShouldIntercept = null;
+
+    private static synchronized @Nullable Method getResourceGatewayShouldIntercept() {
+        if (resourceGatewayResolutionAttempted) {
+            return resourceGatewayShouldIntercept;
+        }
+        resourceGatewayResolutionAttempted = true;
+        try {
+            Class<?> gatewayClass = Class.forName("rs.craby.lonanoterustmodule.LonanoteResourceGateway");
+            resourceGatewayShouldIntercept = gatewayClass.getMethod("shouldIntercept", WebResourceRequest.class);
+        } catch (ReflectiveOperationException | LinkageError ignored) {
+            resourceGatewayShouldIntercept = null;
+        }
+        return resourceGatewayShouldIntercept;
+    }
+
+    protected @Nullable WebResourceResponse interceptLonanoteResource(WebResourceRequest request) {
+        if (!LONANOTE_RESOURCE_SCHEME.equalsIgnoreCase(request.getUrl().getScheme())) {
+            return null;
+        }
+        Method handler = getResourceGatewayShouldIntercept();
+        if (handler == null) {
+            return null;
+        }
+        try {
+            Object response = handler.invoke(null, request);
+            return response instanceof WebResourceResponse ? (WebResourceResponse) response : null;
+        } catch (ReflectiveOperationException | RuntimeException ignored) {
+            return null;
+        }
+    }

     public void setBasicAuthCredential(@Nullable RNCBasicAuthCredential credential) {
         basicAuthCredential = credential;
@@ -138,6 +172,12 @@ public class RNCWebViewClient extends WebViewClient {
         return this.shouldOverrideUrlLoading(view, url);
     }

+    @Override
+    public WebResourceResponse shouldInterceptRequest(WebView view, WebResourceRequest request) {
+        WebResourceResponse response = interceptLonanoteResource(request);
+        return response != null ? response : super.shouldInterceptRequest(view, request);
+    }
+
     @Override
     public void onReceivedHttpAuthRequest(WebView view, HttpAuthHandler handler, String host, String realm) {
         if (basicAuthCredential != null) {
diff --git a/android/src/main/java/com/reactnativecommunity/webview/RNCWebView.java b/android/src/main/java/com/reactnativecommunity/webview/RNCWebView.java
index 80c68034..416d1996 100644
--- a/android/src/main/java/com/reactnativecommunity/webview/RNCWebView.java
+++ b/android/src/main/java/com/reactnativecommunity/webview/RNCWebView.java
@@ -9,6 +9,9 @@ import android.view.Menu;
 import android.view.MenuItem;
 import android.view.MotionEvent;
 import android.view.View;
+import android.view.inputmethod.EditorInfo;
+import android.view.inputmethod.InputConnection;
+import android.view.inputmethod.InputMethodManager;
 import android.webkit.JavascriptInterface;
 import android.webkit.ValueCallback;
 import android.webkit.WebChromeClient;
@@ -77,6 +80,7 @@ public class RNCWebView extends WebView implements LifecycleEventListener {
     private OnScrollDispatchHelper mOnScrollDispatchHelper;
     protected boolean hasScrollEvent = false;
     protected boolean nestedScrollEnabled = false;
+    protected boolean inputMethodEnabled = true;
     protected ProgressChangedFilter progressChangedFilter;

     /**
@@ -107,6 +111,31 @@ public class RNCWebView extends WebView implements LifecycleEventListener {
         this.nestedScrollEnabled = nestedScrollEnabled;
     }

+    public void setInputMethodEnabled(boolean enabled) {
+        inputMethodEnabled = enabled;
+        InputMethodManager inputMethodManager =
+                (InputMethodManager) getContext().getSystemService(android.content.Context.INPUT_METHOD_SERVICE);
+        if (inputMethodManager == null) {
+            return;
+        }
+        inputMethodManager.restartInput(this);
+        if (!enabled) {
+            inputMethodManager.hideSoftInputFromWindow(getWindowToken(), 0);
+        } else if (hasFocus()) {
+            post(() -> inputMethodManager.showSoftInput(this, InputMethodManager.SHOW_IMPLICIT));
+        }
+    }
+
+    @Override
+    public InputConnection onCreateInputConnection(EditorInfo outAttrs) {
+        return inputMethodEnabled ? super.onCreateInputConnection(outAttrs) : null;
+    }
+
+    @Override
+    public boolean onCheckIsTextEditor() {
+        return inputMethodEnabled && super.onCheckIsTextEditor();
+    }
+
     @Override
     public void onHostResume() {
         // do nothing
@@ -465,4 +494,4 @@ public class RNCWebView extends WebView implements LifecycleEventListener {
             return waitingForCommandLoadUrl;
         }
     }
-}
\ No newline at end of file
+}
diff --git a/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt b/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt
index f83033b3..2e41ea94 100644
--- a/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt
+++ b/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewManagerImpl.kt
@@ -547,6 +547,10 @@ class RNCWebViewManagerImpl(private val newArch: Boolean = false) {
         view.settings.javaScriptEnabled = enabled
     }

+    fun setInputMethodEnabled(viewWrapper: RNCWebViewWrapper, enabled: Boolean) {
+        viewWrapper.webView.setInputMethodEnabled(enabled)
+    }
+
     fun setAllowFileAccess(viewWrapper: RNCWebViewWrapper, allowFileAccess: Boolean) {
         val view = viewWrapper.webView
         view.settings.allowFileAccess = allowFileAccess;
diff --git a/android/src/oldarch/com/reactnativecommunity/webview/RNCWebViewManager.java b/android/src/oldarch/com/reactnativecommunity/webview/RNCWebViewManager.java
index 974337f4..262bccf9 100644
--- a/android/src/oldarch/com/reactnativecommunity/webview/RNCWebViewManager.java
+++ b/android/src/oldarch/com/reactnativecommunity/webview/RNCWebViewManager.java
@@ -164,6 +164,11 @@ public class RNCWebViewManager extends ViewGroupManager<RNCWebViewWrapper> {
         mRNCWebViewManagerImpl.setJavaScriptEnabled(view, enabled);
     }

+    @ReactProp(name = "inputMethodEnabled", defaultBoolean = true)
+    public void setInputMethodEnabled(RNCWebViewWrapper view, boolean value) {
+        mRNCWebViewManagerImpl.setInputMethodEnabled(view, value);
+    }
+
     @ReactProp(name = "lackPermissionToDownloadMessage")
     public void setLackPermissionToDownloadMessage(RNCWebViewWrapper view, @Nullable String value) {
         mRNCWebViewManagerImpl.setLackPermissionToDownloadMessage(value);
@@ -332,4 +337,4 @@ public class RNCWebViewManager extends ViewGroupManager<RNCWebViewWrapper> {
         mRNCWebViewManagerImpl.onDropViewInstance(view);
         super.onDropViewInstance(view);
     }
-}
\ No newline at end of file
+}
