import { CompactContentScreen } from "@/components/app_shell";

export function CompactWorkspacesScreen() {
  return (
    <CompactContentScreen description="选择已有工作区，或创建、导入新的工作区。" title="工作区" />
  );
}
