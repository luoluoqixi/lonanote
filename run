#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
MANIFEST_PATH="$SCRIPT_DIR/cli/Cargo.toml"

exec cargo run --manifest-path "$MANIFEST_PATH" --package run -- "$@"
